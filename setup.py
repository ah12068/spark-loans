from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Loan app prediction with spark',
    author='Jeremy Dang',
    license='',
)
